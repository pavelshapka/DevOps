services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      APP_HOST: 0.0.0.0
      APP_PORT: "8080"
      APP_READ_HEADER_TIMEOUT_SEC: "5"
      APP_SHUTDOWN_TIMEOUT_SEC: "10"
    ports:
      - "8080:8080"
    restart: unless-stopped
